version: '3.8'
services:
  cache:
    image: redis:6.2-alpine
    container_name: nest-api-cache
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - cache:/data
volumes:
  api:
    driver: local
  cache:
    driver: local
