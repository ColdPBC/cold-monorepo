{
  "name": "cold-openai",
  "$schema": "/node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/cold-openai/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/cold-openai",
        "main": "apps/cold-openai/src/main.ts",
        "tsConfig": "apps/cold-openai/tsconfig.app.json",
        "assets": ["apps/cold-openai/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/cold-openai/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cold-openai:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cold-openai:build:development"
        },
        "production": {
          "buildTarget": "cold-openai:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/cold-openai/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/cold-openai/jest.config.ts"
      }
    }
  },
  "tags": []
}
