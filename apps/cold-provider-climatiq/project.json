{
  "name": "cold-provider-climatiq",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/cold-provider-climatiq/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/cold-provider-climatiq",
        "main": "apps/cold-provider-climatiq/src/main.ts",
        "tsConfig": "apps/cold-provider-climatiq/tsconfig.app.json",
        "assets": ["apps/cold-provider-climatiq/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/cold-provider-climatiq/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cold-provider-climatiq:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cold-provider-climatiq:build:development"
        },
        "production": {
          "buildTarget": "cold-provider-climatiq:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/cold-provider-climatiq/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/cold-provider-climatiq/jest.config.ts"
      }
    }
  },
  "tags": []
}
